# CodingWorkspace source ref for the CI image build (.github/workflows/build.yml).
# The workflow checks out kevinlb1/CodingWorkspace at this ref (read-only
# CW_DEPLOY_KEY deploy key) and passes it to the build as the `cwsrc` context.
#
# NORMALLY MAINTAINED BY AUTOMATION: track-cw.yml (on main) follows the
# CodingWorkspace `release` branch and bumps this pin — releasing is merging
# to `release`, not editing this file. A manual bump still triggers a build
# (tagged <jupyter-images sha>-cw<codingworkspace sha>), but pause track-cw
# first or it re-bumps within 15 minutes. Full SHAs only, for reproducibility.
# Runbook: PIPELINE.md.
cf42ba206bfe15c0486e61ce533c7027c933ed68
