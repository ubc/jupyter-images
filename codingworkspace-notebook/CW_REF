# CodingWorkspace source ref for the CI image build (.github/workflows/build.yml).
# The workflow checks out kevinlb1/CodingWorkspace at this ref (read-only
# CW_DEPLOY_KEY deploy key) and converts it to a bundle-only `cwsrc` context.
#
# OWNED BY AUTOMATION: track-cw.yml follows the CodingWorkspace `release`
# branch. On jupyter-images main this pin must equal that branch's exact head;
# do not bump it in an image-source PR. Release by advancing CodingWorkspace
# `release`, then let the tracker update this full SHA and dispatch the
# promotion build. Runbook: PIPELINE.md.
958089212463d27f20332434eabf9466855660a4
